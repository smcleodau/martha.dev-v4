# ============================================================================
# Worktree: python-310-work (Index: 7)
# Port Range: 7000-7004
# ============================================================================
# MANAGED BY: Worktree Monitoring Agent
# DO NOT COMMIT THIS FILE
# ============================================================================

WORKTREE_INDEX=7
WORKTREE_NAME=python-310-work
COMPOSE_PROJECT_NAME=archie-python-310-work

# ============================================================================
# Port Configuration (7xxx range)
# ============================================================================
POSTGRES_PORT=7000
REDIS_PORT=7001
API_PORT=7002
PGADMIN_PORT=7003
FRONTEND_PORT=7004

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_DB=archie_python_310_work_dev
DATABASE_URL=postgresql://archie_user:archie_dev_password@localhost:7000/archie_python_310_work_dev
DATABASE_URL_INTERNAL=postgresql://archie_user:archie_dev_password@postgres:5432/archie_python_310_work_dev

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_URL=redis://:archie_dev_redis_password@localhost:7001
REDIS_URL_INTERNAL=redis://:archie_dev_redis_password@redis:6379

# ============================================================================
# Frontend Configuration
# ============================================================================
FRONTEND_URL=http://localhost:7004

# External URLs (Cloudflare Tunnels)

# ============================================================================
# Temporal (Optional - Set if using Temporal workflows)
# ============================================================================
TEMPORAL_CLOUD_ENDPOINT=localhost:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_API_KEY=dev-key
TEMPORAL_TASK_QUEUE=archie-python-310-work
TEMPORAL_CLOUD_NAMESPACE=default
TEMPORAL_CLOUD_ENDPOINT=localhost:7233
TEMPORAL_API_KEY=dev-key
TEMPORAL_TASK_QUEUE_CLOUD_STORAGE=cloud-storage
